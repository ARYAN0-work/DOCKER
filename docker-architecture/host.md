# Docker Architecture – Host

A **Host** is the physical machine, server, or virtual machine where the **Docker Engine** is installed.

It provides the resources (CPU, RAM, storage, and networking) required to run Docker containers.

## Host Can Be

- A physical machine
- A physical server
- A virtual machine (VM)

> **In short:** The Host is the system that runs the Docker Engine and manages Docker containers.